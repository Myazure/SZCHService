<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.milestone.mygeo.dao.IStandardDao">


    <insert id="insertStandard" parameterType="Standard">
        insert into standard(name, code) 
            values(#{name}, #{code})
	</insert>
	

    <update id="createLayerTable" parameterType="map">
		${sql}
	</update>
	
	
    <select id="getStandards"  resultType="Standard" parameterType="Standard">
		select * from standard where name = #{name} or code = #{code}
    </select>
    
    <select id="getAllStandard"  resultType="Standard" >
		select * from standard 
    </select>
    
</mapper>

